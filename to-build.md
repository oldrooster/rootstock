1. Dockerfile - Single file will Uvicorn help?
2. File Upload / Download to Node/VM etc
3. Performance - Realtime
    - Node and VM stats showing CPU, Memory, Disk space
    - Container stats
        - CPU, Memory, bind mount space
    - Recommend other useful stats
    - Needs to be lightweight, how can telemetry be recorded simply? e.g. 1 min snapshots.
4. on Apply, make the ansible command -diff an option that can be selected (or not), and do the same for -v, -vv, -vvv, -vvvv (maybe a drop down on debug level?)
5. Ansible containers --> Pull latest images on all hosts, not sequentially

