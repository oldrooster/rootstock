# Path to the managed homelab repo (inside the container)
HOMELAB_REPO_PATH=/homelab

# Git remote URL for the homelab repo (optional for Phase 1)
HOMELAB_REMOTE_URL=

# CORS origins (comma-separated for production)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Log level
LOG_LEVEL=info
